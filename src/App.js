import {useState} from 'react';
import './App.css';
import receiptData from "./receipData"
import React from 'react';

const App = () => {
  const [receipt, setReceipt] = useState(receiptData)
  const receipts = receiptData.map((receipt, index) => {
  return (
    <>
    className={}
    </>
    
    
  )
  }



export default App;
